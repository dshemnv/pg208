#include <stdio.h>
#include <stdlib.h>
#include <iostream>
#include <vector>
#include <string>

using namespace std;

#include "CBitmap.h"
#include "vecfiles.h"

int main() {
    string filename = "test.vec";
    openfile(filename);
    //int i=0;
    // int a,b,c,d,e,f;
    // char str1[20];
    // char str2[20];
    // ifstream vec_files;
    // string lines="[RECTANGLES : 0, 0, 399, 449, vert pommme, 1 ;]";
    // sscanf(lines.c_str(),"[%s : %d, %d, %d, %d, %[^,], %d, %d ;]",str1,&a,&b,&c,&d,str2,&e,&f);
    // cout<<'['<<str1<<" : "<<a<<", "<<b<<", "<<c<<", "<<d<<", "<<str2<<", "<<e<<", "<<f<<" ;]"<<endl;
    return 1;
}


